# The public origin this deployment is served from, with no trailing slash.
# It is what metadataBase, the OpenGraph and Twitter card URLs, robots.txt and sitemap.xml
# are built from, so link previews point at the wrong host until this is set.
NEXT_PUBLIC_SITE_URL=https://stockroom.example.com
